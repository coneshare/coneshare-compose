# Coneshare Environment Variables Template
#
# Copy this file to .env and fill in the values for your local development setup.
# Do not commit the .env file to version control.

# ------------------------------------------------------------------------------
# Core Django Settings (backend/backend/settings.py)
# ------------------------------------------------------------------------------

# SECURITY WARNING: a secure, randomly generated key is required for production.
SECRET_KEY=django-insecure-development-key-for-coneshare

# Set to "True" for development, "False" for production.
DEBUG=False

# Comma-separated list of allowed hostnames.
ALLOWED_HOSTS=127.0.0.1,localhost,backend

# Comma-separated list of trusted origins for CSRF protection.
CSRF_TRUSTED_ORIGINS=http://localhost:5173

# The public-facing domain of the frontend. Used for generating absolute URLs.
SITE_DOMAIN=http://localhost:5173

# ------------------------------------------------------------------------------
# Database Settings
# ------------------------------------------------------------------------------
# URL format: driver://user:password@host:port/dbname
# Example for PostgreSQL: DATABASE_URL=postgres://coneshare:password@db:5432/coneshare
# Default is SQLite for local development. The path is relative to the container.
DATABASE_URL=postgres://coneshare:password@db:5432/coneshare

# ------------------------------------------------------------------------------
# Redis Settings (Cache & Celery)
# ------------------------------------------------------------------------------
# URL format: redis://:password@host:port/db
# This URL will be used for both the Django cache and the Celery broker.
# If unset, the application defaults to using redis://redis:6379, with
# database 0 for Celery and database 1 for the cache.
#REDIS_URL=redis://redis:6379/0

# ------------------------------------------------------------------------------
# Email Settings (for password resets, notifications, etc.)
# ------------------------------------------------------------------------------
# Use console backend for development to print emails to the console.
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
# For a real SMTP server (e.g., Gmail, SendGrid), configure the following:
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=noreply@example.com

# ------------------------------------------------------------------------------
# File Storage Settings
# ------------------------------------------------------------------------------
# Type of storage to use. Options: FILESYSTEM, MINIO
# For development, FILESYSTEM is recommended.
# MINIO requires a separate MinIO or S3-compatible service.
STORAGE_TYPE=FILESYSTEM

# --- MinIO/S3 Settings (only used if STORAGE_TYPE=MINIO) ---
MINIO_ROOT_USER=
MINIO_ROOT_PASSWORD=
MINIO_BUCKET_NAME=coneshare
# The endpoint URL for your MinIO/S3 service (e.g., http://minio:9000)
MINIO_ENDPOINT=

# ------------------------------------------------------------------------------
# Service Communication (docker-compose.yml)
# ------------------------------------------------------------------------------
# Internal token for secure communication between backend and core services.
INTERNAL_API_TOKEN=supersecrettoken

# URL for the Go file server, as seen from the backend/celery containers.
CORE_API_URL=http://web:8080

# Port for the Go file server.
PORT=8080

# Path inside the core container where files are stored.
STORAGE_PATH=/data
